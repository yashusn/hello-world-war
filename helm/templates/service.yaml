apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-helloworld.fullname" . }}
  labels:
    {{- include "my-helloworld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "my-helloworld.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
