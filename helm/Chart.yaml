apiVersion: v2
name: my-helloworld
description: A Helm chart for the my-helloworld application
type: application
version: 0.1.0          # Chart version — bump when chart changes
appVersion: "1.0.0"   # App version — bump with each release
